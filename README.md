# toebeans

A minimal, extensible personal assistant.

### core concepts

- `server/` - this runs on your box
- `client/` - a basic CLI to connect to the server
- `providers/` - LLM providers
- `plugins/` - separable units that extend functionality
